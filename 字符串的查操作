
/*
 如果想要取得字符串中的相关字符，可以使用下标语法和字符索引。
 当字符索引越界时，会导致运行时错误。
 */
let str = "🐵🐶🐯🐂🐑🐔"

str[str.startIndex]
// str[str.endIndex]

str[str.index(before: str.endIndex)]
str[str.index(after: str.startIndex)]
str[str.index(str.startIndex, offsetBy: 3)]
str[str.index(str.endIndex, offsetBy: -2)]

str[str.startIndex...str.index(str.endIndex, offsetBy: -2)]
str[...str.index(str.endIndex, offsetBy: -2)]

str[str.startIndex..<str.endIndex]
str[str.startIndex...]

/*
 如果想要取得或查找字符串中的相关字符，还可以通过访问其它相关属性或方法。
 */
// 字符串的第一个字符
str.first

// 字符串的最后一个字符
str.last

// 指定的字符在字符串中的索引
str.index(of: Character("🐶"))
str.index(of: Character("🐻"))

// 字符串中是否包含指定的字符
str.contains(Character("🐶"))
str.contains(Character("🐻"))
